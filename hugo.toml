baseURL = "https://ainext.tw/"
languageCode = "zh-TW"
title = "AINEXT"
theme = "newsroom"
author = "James"
canonifyurls = false

enableRobotsTXT = true
buildDrafts = false
buildFuture = true
buildExpired = false

[pagination]
  pagerSize = 20

[params]
  description = "AI 閱讀筆記與產業觀察"
  logo = "images/logo.png"
  logoDark = "images/logo-dark.png"
  # defaultMode = "dark"  # 取消註解可設定預設深色模式
  mainSections = ["posts"]

  # Google Analytics 4 - 填入你的 Measurement ID
  # ga_analytics = "G-XXXXXXXXXX"

  # Google Search Console 驗證 - 填入驗證碼
  # ga_verify = "你的驗證碼"

[outputs]
  home = ["HTML", "RSS", "JSON"]
